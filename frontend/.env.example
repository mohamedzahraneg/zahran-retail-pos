# URL الـ backend API. في الـ Docker: ضيف /api عشان nginx يعمل proxy.
VITE_API_URL=http://localhost:3000

# UUID الافتراضي للمستودع الرئيسي (لازم يطابق seed/migration)
VITE_DEFAULT_WAREHOUSE_ID=

# عدد محاولات المزامنة للفواتير الأوفلاين
VITE_OFFLINE_MAX_RETRY=5
